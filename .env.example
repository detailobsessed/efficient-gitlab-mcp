# =============================================================================
# GitLab MCP Server Configuration
# =============================================================================

# -----------------------------------------------------------------------------
# GitLab API Configuration (Required)
# -----------------------------------------------------------------------------
GITLAB_PERSONAL_ACCESS_TOKEN=your-gitlab-personal-access-token-here
GITLAB_API_URL=https://gitlab.com

# Optional: Default project ID and allowed project IDs
# GITLAB_PROJECT_ID=12345
# GITLAB_ALLOWED_PROJECT_IDS=12345,67890

# Optional: For older GitLab instances with different API behavior
# GITLAB_IS_OLD=false

# Optional: Read-only mode (disables write operations)
# GITLAB_READ_ONLY_MODE=false

# -----------------------------------------------------------------------------
# OAuth Configuration (Alternative to Personal Access Token)
# -----------------------------------------------------------------------------
# GITLAB_USE_OAUTH=true
# GITLAB_OAUTH_CLIENT_ID=your-oauth-client-id
# GITLAB_OAUTH_CLIENT_SECRET=your-oauth-client-secret
# GITLAB_OAUTH_REDIRECT_URI=http://127.0.0.1:8888/callback
# GITLAB_OAUTH_TOKEN_PATH=~/.gitlab-mcp-token.json

# -----------------------------------------------------------------------------
# Transport Mode Configuration
# Priority: STREAMABLE_HTTP > SSE > stdio (default)
# -----------------------------------------------------------------------------
# SSE=false
# STREAMABLE_HTTP=false

# Server host and port (for SSE and Streamable HTTP transports)
HOST=127.0.0.1
PORT=3002

# -----------------------------------------------------------------------------
# HTTP Transport Security (for STREAMABLE_HTTP mode)
# -----------------------------------------------------------------------------
# HTTP_ALLOWED_HOSTS=localhost,127.0.0.1
# HTTP_ALLOWED_ORIGINS=
# HTTP_ENABLE_DNS_REBINDING_PROTECTION=true

# -----------------------------------------------------------------------------
# Remote Authorization (for multi-tenant deployments)
# Note: SSE transport cannot be used with REMOTE_AUTHORIZATION
# -----------------------------------------------------------------------------
# REMOTE_AUTHORIZATION=false
# ENABLE_DYNAMIC_API_URL=false
# SESSION_TIMEOUT_SECONDS=3600
# MAX_SESSIONS=1000
# MAX_REQUESTS_PER_MINUTE=60

# -----------------------------------------------------------------------------
# Feature Flags (Optional tools - disabled by default)
# -----------------------------------------------------------------------------
# USE_GITLAB_WIKI=false
# USE_MILESTONE=false
# USE_PIPELINE=false

# -----------------------------------------------------------------------------
# Logging Configuration
# -----------------------------------------------------------------------------
# LOG_LEVEL=info          # debug, info, warn, error
# LOG_FORMAT=pretty       # json, pretty

# -----------------------------------------------------------------------------
# Proxy Configuration (Optional)
# -----------------------------------------------------------------------------
# HTTP_PROXY=
# HTTPS_PROXY=
# NO_PROXY=localhost,127.0.0.1

# TLS Configuration (for self-signed certificates)
# NODE_TLS_REJECT_UNAUTHORIZED=1
# GITLAB_CA_CERT_PATH=/path/to/ca-cert.pem

# -----------------------------------------------------------------------------
# Test Configuration (for integration tests only)
# -----------------------------------------------------------------------------
# GITLAB_TOKEN_TEST=your-test-token-here
# TEST_PROJECT_ID=your-test-project-id
# ISSUE_IID=1
